const fs=require("fs");const del=require("del");const gulp=require("gulp");const concat=require("gulp-concat");const uglify=require("gulp-uglify");gulp.task("clean",async()=>{del(["*.min.js","!three.min.js"])});gulp.task("scripts",async()=>{const s=fs.readdirSync("src");s.forEach(s=>{const e=s.split(".")[0];return gulp.src([`src/${e}.js`,"lib/*.js"]).pipe(concat(`${e}.min.js`)).pipe(uglify()).pipe(gulp.dest("./"))})});gulp.task("default",gulp.series("clean","scripts"));